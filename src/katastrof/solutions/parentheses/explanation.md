# Parentheses Solution

The reference solution uses a Python list as a stack.

Opening brackets are pushed. Closing brackets must match the latest opening bracket, or
the string is invalid. At the end, the stack must be empty.

Complexity: `O(n)` time and `O(n)` space.

