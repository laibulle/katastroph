# Move Dots Solution

The reference solution builds a compact list of all non-dot values, then appends the
same number of dots that were removed.

This is stable because non-dot values are visited in their original order.

Complexity: `O(n)` time and `O(n)` space.

